# ============================================================
# FILE INI: .env
# Letakkan di ROOT project (sejajar folder config/, login/, dll)
# JANGAN di-commit ke Git! Tambahkan .env ke .gitignore
# ============================================================

DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=inventaris_db
